# .env
# This file is used by Docker Compose to set environment variables for the services.
# Copy this file to .env and fill in your actual values.

# Discord Configuration
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL=

# Google Cloud Configuration (optional)
# If you are using Google Cloud Pub/Sub, provide the path *inside the container* 
# to your service account JSON key file. You'll also need to uncomment the 
# volume mount for it in docker-compose.yml.
# Example: GOOGLE_APPLICATION_CREDENTIALS=/app/src/service_account.json
GOOGLE_APPLICATION_CREDENTIALS=

# You can also override these if needed, otherwise defaults from Dockerfile/docker-compose.yml are used
# PROJECT_ID=your-gcp-project-id
# TOPIC_ID=your-pubsub-topic-id
